WebGL retirement archive created on 2026-04-06.

Purpose:
- Preserve dedicated WebGL build scripts, editor tooling, and plugin glue after retiring WebGL as an active platform.
- Keep Android as the primary validation target and PC standalone as the secondary debugging and scale-validation target.

Contents:
- Archived WebGL-only Unity editor helpers.
- Archived browser Socket.IO bridge plugin files.
- Archived WebGL deployment/upload scripts.

Notes:
- Live runtime and server code were updated to stop depending on the archived assets.
- Inline WebGL branches removed from active files remain recoverable through git history even when no dedicated archive copy exists here.
